import { Box, Typography } from "@mui/material";
export const metadata={
    title:"Product Returns"
}
const Page = () => {
    return (<Box className='p-4 min-h-[50vh]'>
        <Typography variant="h6" pl={1}>My returns</Typography> 
    </Box>);
}
 
export default Page;